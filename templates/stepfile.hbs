{{#each scenarios}}
defineStep(/^{{{name}}}$/, () => {
    {{#each steps}} 
    {{#if func}}
        /** {{{text}}} */ 
        {{{func}}}

    {{else}}
        /**
        * Please create a step for '{{{text}}}'
        */
    {{/if}}
    {{/each}}
})
{{/each}}