/**
* THIS IS AN AUTO GENERATED FILE, PLEASE DO NOT EDIT
*/
{{#each scenarios}}
    defineStep(/^{{{name}}}$/, () => {
    {{#each steps}}

        {
        window.ngui.level(+1);
        window.ngui.nextReusableStepData(`{{{text}}}`, {{#if func}}true{{else}}false{{/if}});

        {{#if func}}
            {{{func}}}
        {{else}}
            /**
            * Please create a step for '{{{text}}}'
            */
        {{/if}}

        window.ngui.level(-1);
        }

    {{/each}}
    })
{{/each}}