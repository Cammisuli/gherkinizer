/**
* THIS IS AN AUTO GENERATED FILE, PLEASE DO NOT EDIT
*/

const __feature = '{{{filename}}}';

describe(`{{{feature}}}`, () => {
{{#each scenarios}}
    {{#is type "Background"}}
        beforeEach(`{{{name}}}`, () => {
        window.ngui.level(0);
        {{#each steps}}
            window.ngui.nextTopStepData(`{{{text}}}`, {{#if func}}true{{else}}false{{/if}});

            {{#if func}}
                /** {{{text}}} */
                {{{func}}};
            {{else}}

                /**
                * Please create a step for '{{{text}}}'
                */
            {{/if}}
        {{/each}}
        })

    {{/is}}
    {{#is type "Scenario"}}
        it(`{{{name}}}`, () => {
        window.ngui.level(0);
        {{#each steps}}
            window.ngui.nextTopStepData(`{{{text}}}`, {{#if func}}true{{else}}false{{/if}});

            {{#if func}}
                /** {{{text}}} */
                {{{func}}}
            {{else}}

                /**
                * Please create a step for '{{{text}}}'
                */
            {{/if}}
        {{/each}}
        });

    {{/is}}
{{/each}}
});