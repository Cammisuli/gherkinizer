describe(`{{{feature}}}`, () => {
    {{#each scenarios}}
    describe(`{{{name}}}`, () => {
        {{#each steps}}
        it(`{{{text}}}`, () => {
            // Fill in your steps.
        })
        {{/each}}
    })
    {{/each}}
});