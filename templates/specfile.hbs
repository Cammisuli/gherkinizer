describe(`{{{feature}}}`, () => {
    {{#each scenarios}}
    describe(`{{{name}}}`, () => {
        {{#each steps}}
        {{#if func}}
        it(`{{{text}}}`, {{{func}}});
        {{else}}
        it(`{{{text}}}`, () => {
            /**
             * Please fill in your steps. Use your steps.js file.
             */
        });
        {{/if}}
        {{/each}}
    });
    {{/each}}
});