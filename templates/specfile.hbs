/**
 * THIS IS AN AUTO GENERATED FILE, PLEASE DO NOT EDIT
 */

describe(`{{{feature}}}`, () => {
    {{#each scenarios}}
    {{#is type "Background"}}
        beforeEach(`{{{name}}}`, () => {
            {{#each steps}} 
            {{#if func}}
               /** {{{text}}} */ 
               {{{func}}};

            {{else}}
                /**
                * Please create a step for '{{{text}}}'
                */
            {{/if}}
            {{/each}}
        })

    {{/is}}
    {{#is type "Scenario"}}
        it(`{{{name}}}`, () => {
            {{#each steps}}
                {{#if func}}
                    /** {{{text}}} */
                    {{{func}}}
                {{else}}
                    
                     /**
                     * Please create a step for '{{{text}}}'
                     */
                {{/if}}
            {{/each}}
        });

    {{/is}}
    {{/each}}
});